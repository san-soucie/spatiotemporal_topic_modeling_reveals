import os
from typing import Callable, Any, TypeVar
from .utils import load_config, PathLikeObject, ConfigDict, FileList
from functools import reduce
import warnings


def main():
    pass

if __name__ == "__main__":
    main()